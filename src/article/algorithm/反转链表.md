```js
const reverseList = (node) => {
  let prev = null;
  while (node) {
    prev = {
      // 将当前节点的值赋值给 prev
      val: node.val,
      // 并设置 next
      next: prev,
    };
    node = node.next;
  }
  return prev;
};
```
